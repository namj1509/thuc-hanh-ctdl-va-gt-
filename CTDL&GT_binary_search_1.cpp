int Binary Search( int a[], int N,int X)
{ int left =0; right=N-1,
  int middle;
  do{
    mid=( left + right)/2;
     if(x=a(middle)) return midle;// Thấy x tại mid
     Else
     if(x<a(midle)right=midle-1;
     Else left+midle+1
}
   while(left<=right);
   return-1; // Tìm hết mà không có x
 } 
